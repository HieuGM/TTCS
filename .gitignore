# 1. Chặn toàn bộ project
/*

# 2. Mở khóa các file/thư mục cần thiết (Whitelist)
!/src/
!/main.py
!/.gitignore
!/person3_resolved.jsonl
#!/requirements.txt

# 3. Chốt chặn các file nhạy cảm (Phải để dưới cùng để đè lên Whitelist trên)
.env
src/.env
**/__pycache__/
*.pyc
**/.env